name: Trigger Vercel Deploy

on:
  push:
    branches:
      - main

jobs:
  trigger-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Trigger Vercel redeploy
        run: |
          echo "Push to main detected - Vercel should auto-deploy"
          echo "Commit: ${{ github.sha }}"
          echo "Timestamp: $(date)"
